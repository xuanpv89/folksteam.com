---
import LoginBtn from '@components/ui/buttons/LoginBtn.astro';

const isVi = Astro.currentLocale === 'vi';
const title = isVi ? 'Thành viên' : 'Login';
const url = isVi ? '/vi/member' : '/member';
---

{/* Login Button */}
<LoginBtn title={title} url={url} />
